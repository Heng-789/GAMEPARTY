# 📦 คู่มือการตั้งค่า Supabase Storage Bucket

## 📋 ภาพรวม

คู่มือนี้จะอธิบายการตั้งค่า Supabase Storage bucket สำหรับ CDN

---

## ✅ การตั้งค่า Bucket: `game-images`

### Bucket Name
```
game-images
```
✅ **ถูกต้องแล้ว** - ตรงกับที่ใช้ในระบบ

---

## 🔧 การตั้งค่า 3 Options

### 1. Public bucket ⭐ **ต้องเปิด (สำคัญมาก!)**

**สถานะ:** ✅ **เปิด (ON)** - **จำเป็นต้องเปิด!**

**เหตุผล:**
- ✅ ต้องเปิดเพื่อให้ Cloudflare Workers และ CDN เข้าถึงไฟล์ได้
- ✅ ต้องเปิดเพื่อให้ frontend โหลดรูปภาพได้โดยไม่ต้อง authentication
- ✅ ต้องเปิดเพื่อให้ public URL ทำงานได้

**ถ้าไม่เปิด:**
- ❌ Cloudflare Workers จะไม่สามารถเข้าถึงไฟล์ได้ (403 Forbidden)
- ❌ CDN URL จะไม่ทำงาน
- ❌ Frontend จะไม่สามารถโหลดรูปภาพได้

**สรุป: ต้องเปิด!** ✅

---

### 2. Restrict file size ⚠️ **ไม่จำเป็น (แต่แนะนำ)**

**สถานะ:** ⚠️ **ปิด (OFF)** หรือ **เปิด (ON)** ก็ได้

**ถ้าเปิด:**
- ✅ ป้องกันการอัปโหลดไฟล์ขนาดใหญ่เกินไป
- ✅ ประหยัด storage space
- ✅ ป้องกันการโหลดช้า

**แนะนำ:**
- เปิดและตั้งค่า limit: **10 MB** (10,485,760 bytes)
- เหมาะสำหรับรูปภาพเกม (JPG, PNG)

**ถ้าไม่เปิด:**
- ⚠️ ผู้ใช้สามารถอัปโหลดไฟล์ขนาดใหญ่ได้
- ⚠️ อาจใช้ storage space มากเกินไป

**สรุป: แนะนำให้เปิดและตั้งค่า 10 MB** ⚠️

---

### 3. Restrict MIME types ⚠️ **ไม่จำเป็น (แต่แนะนำ)**

**สถานะ:** ⚠️ **ปิด (OFF)** หรือ **เปิด (ON)** ก็ได้

**ถ้าเปิด:**
- ✅ ป้องกันการอัปโหลดไฟล์ประเภทอื่นที่ไม่ใช่รูปภาพ
- ✅ เพิ่มความปลอดภัย
- ✅ ป้องกันการอัปโหลดไฟล์อันตราย

**แนะนำ:**
- เปิดและตั้งค่า MIME types:
  ```
  image/jpeg
  image/jpg
  image/png
  image/gif
  image/webp
  ```

**ถ้าไม่เปิด:**
- ⚠️ ผู้ใช้สามารถอัปโหลดไฟล์ประเภทอื่นได้
- ⚠️ อาจมีความเสี่ยงด้านความปลอดภัย

**สรุป: แนะนำให้เปิดและจำกัดเฉพาะรูปภาพ** ⚠️

---

## ✅ สรุปการตั้งค่า

### ต้องเปิด (จำเป็น):
- ✅ **Public bucket**: **ON** ⭐ (สำคัญมาก!)

### แนะนำให้เปิด (ไม่จำเป็น แต่ดีกว่า):
- ⚠️ **Restrict file size**: **ON** (ตั้งค่า 10 MB)
- ⚠️ **Restrict MIME types**: **ON** (จำกัดเฉพาะรูปภาพ)

---

## 📝 ตัวอย่างการตั้งค่า

### แบบขั้นต่ำ (ทำงานได้):
```
Bucket name: game-images
Public bucket: ✅ ON
Restrict file size: ❌ OFF
Restrict MIME types: ❌ OFF
```

### แบบแนะนำ (ปลอดภัยกว่า):
```
Bucket name: game-images
Public bucket: ✅ ON
Restrict file size: ✅ ON (10 MB)
Restrict MIME types: ✅ ON (image/jpeg, image/png, image/gif, image/webp)
```

---

## 🎯 คำแนะนำสำหรับคุณ

### จากรูปที่แสดง:

**ต้องเปิด:**
- ✅ **Public bucket**: **ON** ⭐ (สำคัญมาก!)

**แนะนำให้เปิด:**
- ⚠️ **Restrict file size**: **ON** (ตั้งค่า 10 MB)
- ⚠️ **Restrict MIME types**: **ON** (จำกัดเฉพาะรูปภาพ)

---

## ⚠️ หมายเหตุ

### ถ้าไม่เปิด Public bucket:
- ❌ CDN จะไม่ทำงาน
- ❌ Cloudflare Workers จะได้ 403 Forbidden
- ❌ Frontend จะไม่สามารถโหลดรูปภาพได้

### ถ้าเปิด Public bucket แต่ไม่ตั้งค่า Policy:
- ⚠️ อาจยังไม่ทำงาน
- ⚠️ ต้องตั้งค่า Public Access Policy ด้วย (ดู `NEXT-STEPS-AFTER-NAMESERVERS.md`)

---

## ✅ Checklist

- [ ] Bucket name: `game-images`
- [ ] Public bucket: ✅ **ON** (ต้องเปิด!)
- [ ] Restrict file size: ⚠️ ON (แนะนำ: 10 MB)
- [ ] Restrict MIME types: ⚠️ ON (แนะนำ: image/*)
- [ ] คลิก **Create**

---

**สรุป: ต้องเปิด Public bucket เท่านั้น ส่วนอีก 2 ตัวแนะนำให้เปิดเพื่อความปลอดภัย**

