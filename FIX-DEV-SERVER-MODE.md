# 🔧 แก้ไข: Dev Server ไม่โหลด Environment Variables

## ❌ ปัญหา

Error: `DNS_PROBE_FINISHED_NXDOMAIN` สำหรับ `cdn.heng36.party`

**สาเหตุ:**
- ใช้ `npm run dev` ซึ่งไม่ได้ระบุ `--mode heng36`
- Vite ไม่โหลด `env.heng36` เพราะไม่ได้ระบุ mode
- ระบบใช้ fallback `cdn.heng36.party` แทน `img.heng36.party`

---

## ✅ วิธีแก้ไข

### วิธีที่ 1: ใช้ Script ที่ถูกต้อง (แนะนำ)

**ใช้ `npm run dev:heng36` แทน `npm run dev`:**

```bash
npm run dev:heng36
```

**Script นี้จะ:**
- ✅ ใช้ `--mode heng36` → โหลด `env.heng36`
- ✅ ใช้ port 5173
- ✅ Environment variables ถูกโหลดถูกต้อง

---

### วิธีที่ 2: แก้ไข `dev` Script (ถ้าต้องการใช้ `npm run dev`)

**แก้ไข `package.json`:**

```json
{
  "scripts": {
    "dev": "vite --mode heng36",  // ✅ เพิ่ม --mode heng36
    "dev:heng36": "vite --mode heng36 --port 5173",
    ...
  }
}
```

**แล้ว restart dev server:**
```bash
npm run dev
```

---

## 🔍 ตรวจสอบว่า Environment Variables ถูกโหลด

**หลังจาก restart dev server:**

1. เปิด Browser Console (F12)
2. อัปโหลดรูปภาพ
3. ดู Console log:
   ```
   🔍 CDN Config: {
     theme: 'heng36',
     domain: 'img.heng36.party',  ✅ ถูกต้อง
     ...
   }
   ```

**ถ้าเห็น:**
- ✅ `domain: 'img.heng36.party'` → Environment variable ถูกโหลดแล้ว
- ❌ `domain: 'cdn.heng36.party'` → Environment variable ไม่ได้ถูกโหลด

---

## 📋 Checklist

- [ ] ใช้ `npm run dev:heng36` แทน `npm run dev`
- [ ] หรือแก้ไข `dev` script ให้ใช้ `--mode heng36`
- [ ] Restart dev server
- [ ] ตรวจสอบ Console log ว่า domain เป็น `img.heng36.party`
- [ ] ทดสอบอัปโหลดรูปภาพใหม่

---

## 🎯 สรุป

**สาเหตุ:** ใช้ `npm run dev` ซึ่งไม่ได้ระบุ mode → ไม่โหลด `env.heng36`

**วิธีแก้:** ใช้ `npm run dev:heng36` หรือแก้ไข `dev` script

**เวลาที่ใช้:** ~2 นาที

---

**ต้องการความช่วยเหลือเพิ่มเติมไหม?**

