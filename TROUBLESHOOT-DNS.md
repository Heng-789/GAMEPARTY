# 🔧 แก้ไขปัญหา DNS: Non-existent domain

## ❌ ปัญหาที่พบ

```bash
nslookup img.heng36.party
*** dns.google can't find img.heng36.party: Non-existent domain
```

**หมายความว่า:** DNS record ยังไม่มีหรือยังไม่ propagate

---

## 🔍 สาเหตุที่เป็นไปได้

### 1. ยังไม่ได้ตั้งค่า CNAME Record ใน Cloudflare
- ❌ ยังไม่ได้สร้าง CNAME record สำหรับ `img.heng36.party`
- ✅ **วิธีแก้:** ตั้งค่า CNAME record ใน Cloudflare Dashboard

### 2. Nameservers ยังไม่ Propagate เสร็จ
- ❌ Domain ยังไม่ได้เปลี่ยน Nameservers เป็น Cloudflare
- ⏳ กำลังรอ Nameservers propagate (24-48 ชั่วโมง)
- ✅ **วิธีแก้:** รอ Nameservers propagate แล้วค่อยตั้งค่า CNAME

### 3. DNS ยังไม่ Propagate
- ⏳ ตั้งค่า CNAME แล้ว แต่ยังไม่ propagate (5-15 นาที)
- ✅ **วิธีแก้:** รอ 5-15 นาที แล้วทดสอบใหม่

---

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบ Nameservers

**ตรวจสอบว่า domain ใช้ Cloudflare Nameservers แล้วหรือยัง:**

```powershell
nslookup -type=NS heng36.party
```

**ผลลัพธ์ที่ควรได้ (ถ้าใช้ Cloudflare):**
```
heng36.party nameserver = dante.ns.cloudflare.com
heng36.party nameserver = kira.ns.cloudflare.com
```

**ถ้าได้ผลลัพธ์นี้ = ใช้ Cloudflare อยู่แล้ว** ✅

**ถ้าได้ nameservers อื่น = ยังไม่ได้ใช้ Cloudflare** ⚠️

---

### ขั้นตอนที่ 2: ตรวจสอบใน Cloudflare Dashboard

1. เข้า https://dash.cloudflare.com
2. ดูรายการ domains
3. ตรวจสอบว่า `heng36.party` มี status เป็น **Active** (ส้ม) หรือไม่

**ถ้าเป็น Active = ใช้ Cloudflare อยู่แล้ว** ✅  
**ถ้ายังไม่มี domain หรือ status ไม่ใช่ Active = ยังไม่ได้ใช้ Cloudflare** ⚠️

---

### ขั้นตอนที่ 3: ตั้งค่า CNAME Record (ถ้า Nameservers propagate แล้ว)

**ถ้า Nameservers propagate แล้ว:**

1. เข้า Cloudflare Dashboard → เลือก domain `heng36.party`
2. ไปที่ **DNS** → **Records**
3. ตรวจสอบว่ามี CNAME record สำหรับ `img` หรือไม่
4. ถ้ายังไม่มี → คลิก **Add record**
5. ตั้งค่า:
   - **Type**: `CNAME`
   - **Name**: `img`
   - **Target**: `ipflzfxezdzbmoqglknu.supabase.co`
   - **Proxy status**: ✅ **Proxied** (ส้ม)
   - **TTL**: Auto
6. คลิก **Save**

---

### ขั้นตอนที่ 4: รอ DNS Propagate

**หลังจากตั้งค่า CNAME:**
- ⏳ รอ 5-15 นาที
- 🔄 DNS จะ propagate ไปทั่วโลก

**ทดสอบใหม่:**
```powershell
nslookup img.heng36.party
```

**ผลลัพธ์ที่ควรได้:**
```
img.heng36.party canonical name = ipflzfxezdzbmoqglknu.supabase.co
```

---

## 📋 Checklist การแก้ไข

### ถ้า Nameservers ยังไม่ Propagate:
- [ ] รอ Nameservers propagate (24-48 ชั่วโมง)
- [ ] ตรวจสอบใน Cloudflare Dashboard ว่า domain status เป็น **Active**
- [ ] หลังจากนั้นค่อยตั้งค่า CNAME

### ถ้า Nameservers Propagate แล้ว:
- [ ] เข้า Cloudflare Dashboard → DNS → Records
- [ ] ตรวจสอบว่ามี CNAME record สำหรับ `img` หรือไม่
- [ ] ถ้ายังไม่มี → สร้าง CNAME record:
  - Name: `img`
  - Target: `ipflzfxezdzbmoqglknu.supabase.co`
  - Proxy: Proxied (ส้ม)
- [ ] รอ 5-15 นาที
- [ ] ทดสอบใหม่: `nslookup img.heng36.party`

---

## 🧪 วิธีทดสอบ

### 1. ทดสอบ Nameservers
```powershell
nslookup -type=NS heng36.party
```

### 2. ทดสอบ CNAME Record
```powershell
nslookup img.heng36.party
```

### 3. ทดสอบด้วย DNS Server อื่น
```powershell
nslookup img.heng36.party 8.8.8.8
nslookup img.heng36.party 1.1.1.1
```

---

## ⚠️ หมายเหตุ

### ถ้ายังรอ Nameservers Propagate:
- ⏳ **ไม่ต้องตั้งค่า CNAME ตอนนี้** - จะไม่ทำงาน
- ⏳ รอ Nameservers propagate เสร็จก่อน (24-48 ชั่วโมง)
- ⏳ หลังจากนั้นค่อยตั้งค่า CNAME

### ถ้า Nameservers Propagate แล้ว:
- ✅ ตั้งค่า CNAME ได้เลย
- ✅ รอ 5-15 นาที แล้วทดสอบใหม่

---

## 📝 สรุป

**จากผลลัพธ์ที่ได้:**
```
*** dns.google can't find img.heng36.party: Non-existent domain
```

**สาเหตุที่เป็นไปได้:**
1. ⏳ **Nameservers ยังไม่ propagate** (ถ้ายังรออยู่)
2. ❌ **ยังไม่ได้ตั้งค่า CNAME record** (ถ้า Nameservers propagate แล้ว)

**วิธีแก้:**
1. ตรวจสอบ Nameservers: `nslookup -type=NS heng36.party`
2. ถ้าใช้ Cloudflare อยู่แล้ว → ตั้งค่า CNAME record
3. ถ้ายังไม่ได้ใช้ Cloudflare → รอ Nameservers propagate ก่อน

---

**ต้องการความช่วยเหลือเพิ่มเติมไหม?**

