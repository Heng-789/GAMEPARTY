# Test Script: ตรวจสอบการแจกโค้ดซ้ำกัน

## 📋 คำอธิบาย

สคริปต์นี้ใช้สำหรับทดสอบว่าระบบแจกโค้ดในเกมเช็คอินทำงานถูกต้องหรือไม่ โดยเฉพาะการป้องกันการแจกโค้ดซ้ำกันเมื่อ USER หลายคนเช็คอินพร้อมกัน

## 🚀 วิธีใช้งาน

### 1. ติดตั้ง dependencies (ถ้ายังไม่ได้ติดตั้ง)

```bash
npm install
```

### 2. ตั้งค่า Environment Variables

สร้างไฟล์ `.env.local` ใน root directory และเพิ่ม:

```env
VITE_FIREBASE_API_KEY=your_api_key
VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain
VITE_FIREBASE_DATABASE_URL=your_database_url
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id
```

### 3. รันสคริปต์ทดสอบ

```bash
npx tsx scripts/test-checkin-code-duplicate.ts <gameId> <dayIndex> <numUsers>
```

### ตัวอย่างการใช้งาน

```bash
# ทดสอบกับ 10 users เช็คอินพร้อมกันในวันแรก
npx tsx scripts/test-checkin-code-duplicate.ts game_123 0 10

# ทดสอบกับ 50 users เช็คอินพร้อมกันในวันที่สอง
npx tsx scripts/test-checkin-code-duplicate.ts game_123 1 50

# ทดสอบกับ 100 users เช็คอินพร้อมกันในวันแรก
npx tsx scripts/test-checkin-code-duplicate.ts game_123 0 100
```

## 📊 ผลลัพธ์ที่คาดหวัง

### ✅ กรณีที่ระบบทำงานถูกต้อง:
- ไม่พบโค้ดซ้ำกัน
- ทุก User ได้รับโค้ด (ถ้ามีโค้ดเพียงพอ)
- แสดงเวลาที่ใช้ในการทดสอบ

### ❌ กรณีที่ระบบมีปัญหา:
- พบโค้ดซ้ำกัน (แสดงรายละเอียดว่าโค้ดไหนซ้ำกัน)
- บาง User ไม่ได้รับโค้ด (ถ้าโค้ดไม่พอ)

## 🔍 วิธีการทำงาน

1. **สร้าง User หลายคน**: สร้าง user IDs ตามจำนวนที่ระบุ
2. **เช็คอินพร้อมกัน**: ใช้ `Promise.all()` เพื่อจำลองการเช็คอินพร้อมกัน
3. **ตรวจสอบผลลัพธ์**: 
   - ตรวจสอบว่าโค้ดไม่ซ้ำกัน
   - ตรวจสอบว่าโค้ดถูกแจกตามลำดับ
   - แสดงสถิติการทดสอบ

## ⚠️ ข้อควรระวัง

1. **จำนวนโค้ด**: ควรมีโค้ดเพียงพอสำหรับจำนวน User ที่ทดสอบ
2. **Game ID**: ต้องเป็น Game ID ที่มีอยู่จริงในระบบ
3. **Day Index**: ต้องเป็นวันที่มีโค้ดตั้งค่าไว้
4. **Environment**: ต้องตั้งค่า Firebase config ให้ถูกต้อง

## 📝 ตัวอย่างผลลัพธ์

```
🧪 เริ่มทดสอบการแจกโค้ดซ้ำกัน...

📋 ข้อมูลการทดสอบ:
   - Game ID: game_123
   - Day Index: 0
   - จำนวน User: 10
   - โหมด: เช็คอินพร้อมกัน (Parallel)

📦 จำนวนโค้ดที่มี: 100
👥 จำนวน User ที่จะทดสอบ: 10

📅 Server Date: 2024-01-15

🚀 เริ่มทดสอบการเช็คอินพร้อมกัน...

📊 ผลลัพธ์การทดสอบ:

   ✅ สำเร็จ: 10/10
   ❌ ล้มเหลว: 0/10
   ⏱️  เวลาที่ใช้: 1234ms
   📦 โค้ดที่ได้รับ: 10
   🔢 โค้ดที่ไม่ซ้ำ: 10
   🔁 โค้ดที่ซ้ำ: 0

📋 รายละเอียดผลลัพธ์:

   ✅ User 1 (test_user_1): CODE001 [123ms]
   ✅ User 2 (test_user_2): CODE002 [125ms]
   ✅ User 3 (test_user_3): CODE003 [127ms]
   ...

🔍 ตรวจสอบการซ้ำกัน:

   ✅ ไม่พบโค้ดซ้ำกัน - ระบบทำงานถูกต้อง!

📝 สรุปผล:

   ✅ ทดสอบผ่าน: ไม่พบโค้ดซ้ำกัน และทุก User ได้รับโค้ด
```

## 🛠️ Troubleshooting

### ปัญหา: ไม่พบโค้ดใน game
**วิธีแก้**: ตรวจสอบว่า game มีโค้ดตั้งค่าไว้ใน `games/{gameId}/checkin/rewardCodes/{dayIndex}`

### ปัญหา: Firebase connection error
**วิธีแก้**: ตรวจสอบว่า environment variables ถูกตั้งค่าถูกต้อง

### ปัญหา: TypeScript compilation error
**วิธีแก้**: ตรวจสอบว่า TypeScript version ตรงกับที่กำหนดใน package.json

