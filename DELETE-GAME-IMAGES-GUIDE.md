# 📋 คู่มือการลบรูปภาพเมื่อลบเกม

## ✅ สิ่งที่ทำเสร็จแล้ว

### 1. สร้าง Utility Functions (`backend/src/utils/storage.js`)

- ✅ `getSupabaseClient(theme)` - สร้าง Supabase client สำหรับแต่ละ theme
- ✅ `getStorageBucket(theme)` - ดึงชื่อ bucket สำหรับแต่ละ theme
- ✅ `extractStoragePathFromCDN(cdnUrl, theme)` - แปลง CDN URL เป็น storage path
- ✅ `deleteImageFromStorage(cdnUrl, theme)` - ลบรูปภาพจาก Supabase Storage
- ✅ `extractImageUrlsFromGameData(gameData)` - ดึง URL รูปภาพทั้งหมดจาก game data

### 2. แก้ไข DELETE Endpoint (`backend/src/routes/games.js`)

- ✅ ดึงข้อมูลเกมก่อนลบ (เพื่อดึง URL รูปภาพ)
- ✅ ดึง URL รูปภาพทั้งหมดจาก `game_data` JSONB
- ✅ ลบรูปภาพจาก Supabase Storage ก่อนลบเกม
- ✅ ใช้ `Promise.allSettled` เพื่อให้ลบเกมต่อได้แม้บางรูปลบไม่สำเร็จ

---

## 📊 รูปภาพที่รองรับการลบ

ระบบจะลบรูปภาพจากฟิลด์ต่อไปนี้:

1. **เกมทายภาพปริศนา**
   - `puzzle.imageDataUrl`

2. **เกมทายเบอร์เงิน**
   - `numberPick.imageDataUrl`

3. **เกมทายผลบอล**
   - `football.imageDataUrl`

4. **เกมเช็คอิน**
   - `checkin.image` (รูปภาพหลัก)
   - `checkin.announceImage` (รูปภาพประกาศ)

5. **เกมลอยกระทง**
   - `loyKrathong.image`

6. **เกม BINGO**
   - `bingo.image`

7. **เกม Trick or Treat**
   - `trickOrTreat.ghostImage`

---

## 🔧 การตั้งค่า Environment Variables

ต้องตั้งค่า environment variables สำหรับ Supabase ใน backend:

```env
# สำหรับ HENG36
SUPABASE_URL_HENG36=https://xxxxx.supabase.co
SUPABASE_ANON_KEY_HENG36=your_anon_key

# สำหรับ MAX56
SUPABASE_URL_MAX56=https://xxxxx.supabase.co
SUPABASE_ANON_KEY_MAX56=your_anon_key

# สำหรับ JEED24
SUPABASE_URL_JEED24=https://xxxxx.supabase.co
SUPABASE_ANON_KEY_JEED24=your_anon_key

# Fallback (ถ้าไม่มี theme-specific keys)
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_ANON_KEY=your_anon_key
```

---

## 📦 การติดตั้ง Dependencies

รันคำสั่งต่อไปนี้เพื่อติดตั้ง `@supabase/supabase-js`:

```bash
cd backend
npm install @supabase/supabase-js
```

---

## 🎯 วิธีการทำงาน

1. **เมื่อลบเกม:**
   - ระบบจะดึงข้อมูลเกมจาก PostgreSQL
   - ดึง URL รูปภาพทั้งหมดจาก `game_data` JSONB
   - ลบรูปภาพจาก Supabase Storage (ใช้ `Promise.allSettled` เพื่อให้ลบต่อได้แม้บางรูปลบไม่สำเร็จ)
   - ลบข้อมูลเกมและข้อมูลที่เกี่ยวข้องจาก PostgreSQL

2. **การจัดการ Error:**
   - ถ้าลบรูปภาพไม่สำเร็จ ระบบจะ log error แต่จะลบเกมต่อได้
   - ใช้ `Promise.allSettled` เพื่อให้ไม่ block การลบเกม

---

## ⚠️ หมายเหตุ

- ✅ ระบบจะข้าม `data:` URLs และ `blob:` URLs (ไม่ต้องลบ)
- ✅ ระบบจะลบเฉพาะรูปภาพที่อยู่ใน Supabase Storage (CDN URLs)
- ✅ ถ้าไม่มีรูปภาพ ระบบจะลบเกมตามปกติ
- ✅ ถ้าลบรูปภาพไม่สำเร็จ ระบบจะลบเกมต่อได้ (ไม่ block)

---

## 🧪 การทดสอบ

1. สร้างเกมที่มีรูปภาพ
2. ตรวจสอบว่ารูปภาพถูกอัปโหลดไปที่ Supabase Storage
3. ลบเกม
4. ตรวจสอบว่ารูปภาพถูกลบจาก Supabase Storage

---

**📌 หมายเหตุ:** ต้องติดตั้ง `@supabase/supabase-js` และตั้งค่า environment variables ก่อนใช้งาน

