# สรุปการทำงานของ "เกมประกาศรางวัล"

## 📋 ภาพรวม
เกมประกาศรางวัลเป็นเกมที่ใช้ประกาศรายชื่อผู้ได้รับรางวัลโบนัสประจำเดือน โดยผู้เล่นจะกรอก USER เพื่อตรวจสอบว่าตัวเองได้รับรางวัลหรือไม่

---

## 🔧 1. การสร้างเกม (CreateGame.tsx)

### 1.1 การอัปโหลดข้อมูล
- **อัปโหลด CSV/XLSX**: อัปโหลดไฟล์ที่มีรายชื่อ USER และจำนวนโบนัส
  - รูปแบบ: `USER,BONUS` (เช่น `YJMAX,2000`)
  - ข้อมูลจะถูก parse และเก็บใน state:
    - `announceUsers`: Array ของ USER names
    - `announceUserBonuses`: Array ของ `{user: string, bonus: number}`

- **อัปโหลดรูปภาพ**: อัปโหลดรูปภาพประกาศรางวัล
  - เก็บใน `announceImageDataUrl` (CDN URL)
  - เก็บชื่อไฟล์ใน `announceFileName`

### 1.2 การบันทึกข้อมูล
เมื่อบันทึกเกม ข้อมูลจะถูกบันทึกลงฐานข้อมูลในรูปแบบ:
```json
{
  "announce": {
    "users": ["USER1", "USER2", ...],
    "userBonuses": [
      {"user": "USER1", "bonus": 2000},
      {"user": "USER2", "bonus": 1000}
    ],
    "imageDataUrl": "https://...",
    "fileName": "แจกรางวัล.jpg",
    "processedItems": {} // เก็บข้อมูลการทำรายการจาก Admin
  }
}
```

**หมายเหตุ**: 
- Frontend จะไม่ส่ง `announce` ถ้า `users` และ `userBonuses` เป็น array ว่าง
- Backend จะไม่ใช้ array ว่างทับข้อมูลเดิม (ป้องกันข้อมูลหาย)

---

## 🎮 2. การเล่นเกม (GamePlay.tsx)

### 2.1 การตรวจสอบสิทธิ์
1. ผู้เล่นกรอก USER name
2. ระบบตรวจสอบ:
   - **สิทธิ์การเข้าเล่น**: ถ้า `userAccessType === 'selected'` จะตรวจสอบว่า USER อยู่ใน `selectedUsers` หรือไม่
   - **รายชื่อผู้ได้รับรางวัล**: ตรวจสอบว่า USER อยู่ใน `announce.users` หรือไม่

### 2.2 ผลลัพธ์

#### ✅ ถ้า USER อยู่ในรายชื่อผู้ได้รับรางวัล:
- แสดง `AnnounceGame` component
- แสดงข้อความ: "🎉 ยินดีด้วย! {USER} ได้รับรางวัลโบนัสประจำเดือน {BONUS} 🎉"
- แสดงรูปภาพประกาศรางวัล (ถ้ามี)
- แสดงวิธีการเช็คโบนัส
- แสดงปุ่ม "ไปที่ HENG36/MAX56/JEED24"

#### ❌ ถ้า USER ไม่อยู่ในรายชื่อ:
- แสดง popup ข้อความตามธีม:
  - **HENG36**: "ลูกค้ายังไม่เข้าเงื่อนไขที่ได้รับรางวัลประจำเดือนนะคะ... เงื่อนไข ต้องมียอดฝากสูงสุด 20,000 USER ของ HENG36..."
  - **MAX56**: "ลูกค้ายังไม่เข้าเงื่อนไขที่ได้รับรางวัลประจำเดือนนะคะ... เงื่อนไข ต้องมียอดฝากสูงสุด 3,000 USER ของ MAX56..."
  - **JEED24**: "ลูกค้ายังไม่เข้าเงื่อนไขที่ได้รับรางวัลประจำเดือนนะคะ... เงื่อนไข ต้องมียอดฝากสูงสุด 20,000 USER ของ JEED24..."

---

## 👨‍💼 3. การจัดการในหน้า Admin (AdminAnswers.tsx)

### 3.1 การแสดงรายชื่อ
- แสดงรายชื่อ USER ทั้งหมดที่อัปโหลดมา
- แสดงจำนวนโบนัสของแต่ละ USER
- แสดงปุ่ม "ทำรายการ" สำหรับแต่ละรายการ

### 3.2 การทำรายการ (Process Items)
1. Admin กดปุ่ม "ทำรายการ" ของรายการที่ต้องการ
2. ระบบเปิด input field ให้กรอกข้อมูล (เช่น CODE หรือข้อมูลอื่นๆ)
3. Admin กรอกข้อมูลและกดบันทึก
4. ข้อมูลจะถูกบันทึกลง `announce.processedItems`:
   ```json
   {
     "processedItems": {
       "USER1": {
         "value": "CODE123",
         "timestamp": 1764605159514
       }
     }
   }
   ```
5. ข้อมูลจะถูกบันทึกในฐานข้อมูลและแสดงใน UI ทันที

### 3.3 การโหลดข้อมูล
- ใช้ `fullData=true` เพื่อบังคับให้ backend ส่งข้อมูลเต็มจากฐานข้อมูล
- รองรับทั้ง nested structure (`gameData.announce`) และ flat structure (`announce`)
- แปลง `users` และ `userBonuses` จาก object เป็น array (ถ้าจำเป็น)
- ป้องกัน state reset ที่ไม่จำเป็น

---

## 💾 4. การบันทึกข้อมูลในฐานข้อมูล (Backend)

### 4.1 โครงสร้างข้อมูล
- **Table**: `games`
- **Column**: `game_data` (JSONB type)
- **Structure**: 
  ```json
  {
    "announce": {
      "users": [...],
      "userBonuses": [...],
      "imageDataUrl": "...",
      "fileName": "...",
      "processedItems": {...}
    }
  }
  ```

### 4.2 การ Merge ข้อมูล
เมื่อบันทึกเกม:
1. อ่านข้อมูลเดิมจากฐานข้อมูล
2. Merge ข้อมูลใหม่กับข้อมูลเดิม:
   - **users/userBonuses**: ใช้ข้อมูลใหม่ถ้าไม่ว่าง, ถ้าว่างใช้ของเดิม
   - **imageDataUrl/fileName**: ใช้ข้อมูลใหม่ถ้ามี
   - **processedItems**: Deep merge (ไม่ลบข้อมูลเดิม)

### 4.3 การป้องกันข้อมูลหาย
- ตรวจสอบว่า array ไม่ว่างก่อนใช้ค่าที่ส่งมา
- ถ้า frontend ส่ง array ว่างมา จะใช้ข้อมูลเดิมแทน
- Frontend จะไม่ส่ง `announce` ถ้าไม่มีข้อมูลจริง

---

## 🔄 5. Flow การทำงานทั้งหมด

```
1. Admin สร้างเกมประกาศรางวัล
   ↓
2. อัปโหลด CSV/XLSX (รายชื่อ USER + โบนัส)
   ↓
3. อัปโหลดรูปภาพประกาศรางวัล
   ↓
4. บันทึกเกม → ข้อมูลถูกบันทึกในฐานข้อมูล
   ↓
5. ผู้เล่นกรอก USER → ตรวจสอบสิทธิ์
   ↓
6. ถ้าอยู่ในรายชื่อ → แสดงผลรางวัล
   ถ้าไม่อยู่ในรายชื่อ → แสดงข้อความไม่เข้าเงื่อนไข
   ↓
7. Admin ดูรายชื่อในหน้า AdminAnswers
   ↓
8. Admin ทำรายการ (กรอก CODE/ข้อมูล) → บันทึกลง processedItems
```

---

## 🛡️ 6. การป้องกันปัญหา

### 6.1 ป้องกันข้อมูลหาย
- Backend ไม่ใช้ array ว่างทับข้อมูลเดิม
- Frontend ไม่ส่ง `announce` ถ้าไม่มีข้อมูลจริง
- ใช้ deep merge สำหรับ `processedItems`

### 6.2 ป้องกัน State Reset
- ใช้ `useRef` เพื่อเก็บ `gameId` และ `gameType` ป้องกันการ reset ที่ไม่จำเป็น
- ตรวจสอบว่า state เปลี่ยนจริงๆ ก่อนอัปเดต
- Reload ข้อมูล announce เมื่อ `gameData` เปลี่ยน

### 6.3 Debug Logging
- Frontend: Log ข้อมูลที่ส่งไป backend
- Backend: Log ข้อมูลที่ merge และบันทึก
- AdminAnswers: Log ข้อมูลที่โหลดมา

---

## 📝 7. ข้อมูลสำคัญ

### 7.1 State Variables
- `announceUsers`: Array ของ USER names
- `announceUserBonuses`: Array ของ `{user: string, bonus: number}`
- `announceImageDataUrl`: URL ของรูปภาพประกาศ
- `announceFileName`: ชื่อไฟล์รูปภาพ
- `editingItems`: ข้อมูลการแก้ไขรายการ
- `processedItems`: ข้อมูลที่ทำรายการแล้ว

### 7.2 API Endpoints
- `GET /games/:gameId?full=true`: โหลดข้อมูลเกม (full data)
- `PUT /games/:gameId`: อัปเดตข้อมูลเกม (merge announce data)

---

## ✅ 8. Checklist การทำงาน

- [x] อัปโหลด CSV/XLSX ได้
- [x] อัปโหลดรูปภาพได้
- [x] บันทึกข้อมูลลงฐานข้อมูลได้
- [x] โหลดข้อมูลจากฐานข้อมูลได้
- [x] ตรวจสอบสิทธิ์ผู้เล่นได้
- [x] แสดงผลรางวัลได้
- [x] แสดงข้อความไม่เข้าเงื่อนไขได้
- [x] Admin ทำรายการได้
- [x] ป้องกันข้อมูลหาย
- [x] ป้องกัน state reset

