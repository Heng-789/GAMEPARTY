{
  "rules": {
    // ==========================================
    // ✅ INDEXES - สำหรับ Optimization
    // ==========================================
    
    // ✅ 1. BingoGame Cards Index (Phase 1) - Critical
    // Path: games/{gameId}/bingo/cards
    // Index Key: userId
    // วัตถุประสงค์: Query เฉพาะการ์ดของตัวเอง
    "games": {
      "$gameId": {
        "bingo": {
          "cards": {
            ".indexOn": ["userId"]
          }
        }
      }
    },
    
    // ✅ 2. LiveChat Index (Phase 2) - Critical
    // Path: chat/{gameId}
    // Index Key: timestamp
    // วัตถุประสงค์: Query ข้อความล่าสุด 50 ข้อความ
    "chat": {
      "$gameId": {
        ".indexOn": ["timestamp"]
      }
    },
    
    // ✅ 3. LoyKrathongGame Krathongs Index (Phase 6) - High Priority
    // Path: krathongs/{gameId}/recent
    // Index Key: timestamp
    // วัตถุประสงค์: Query กระทงล่าสุด 50 กระทง
    "krathongs": {
      "$gameId": {
        "recent": {
          ".indexOn": ["timestamp"]
        }
      }
    },
    
    // ==========================================
    // 🔒 SECURITY RULES - Rules ที่มีอยู่แล้ว
    // ==========================================
    // ⚠️ หมายเหตุ: Rules ด้านล่างนี้เป็นตัวอย่าง
    // ⚠️ ต้องปรับแต่งตามความต้องการของโปรเจกต์ของคุณ
    
    // ตัวอย่าง 1: อนุญาตให้ทุกคนอ่านและเขียน (สำหรับ development เท่านั้น!)
    // ⚠️ ⚠️ ⚠️ อย่าใช้ใน production!
    ".read": true,
    ".write": true
    
    // ตัวอย่าง 2: อนุญาตเฉพาะ authenticated users
    // ".read": "auth != null",
    // ".write": "auth != null"
    
    // ตัวอย่าง 3: Rules ที่ละเอียดขึ้น (แนะนำสำหรับ production)
    // ".read": "auth != null",
    // ".write": "auth != null",
    // 
    // "games": {
    //   "$gameId": {
    //     ".read": true,  // ทุกคนอ่านได้
    //     ".write": "auth != null && auth.uid === data.child('createdBy').val()",  // เฉพาะเจ้าของแก้ไขได้
    //     
    //     "bingo": {
    //       "cards": {
    //         ".indexOn": ["userId"],  // ✅ Index ที่เพิ่มเข้าไป
    //         "$cardId": {
    //           ".read": true,
    //           ".write": "auth != null"
    //         }
    //       }
    //     }
    //   }
    // },
    // 
    // "chat": {
    //   "$gameId": {
    //     ".indexOn": ["timestamp"],  // ✅ Index ที่เพิ่มเข้าไป
    //     "$messageId": {
    //       ".read": true,
    //       ".write": "auth != null"
    //     }
    //   }
    // },
    // 
    // "krathongs": {
    //   "$gameId": {
    //     "recent": {
    //       ".indexOn": ["timestamp"],  // ✅ Index ที่เพิ่มเข้าไป
    //       "$krathongId": {
    //         ".read": true,
    //         ".write": "auth != null"
    //       }
    //     }
    //   }
    // }
  }
}

