{
  "rules": {
    // ⚠️ หมายเหตุ: ไฟล์นี้เป็นตัวอย่างเท่านั้น
    // ⚠️ ต้องปรับแต่ง rules ตามความต้องการของโปรเจกต์ของคุณ
    // ⚠️ Rules ที่มีอยู่แล้วให้เก็บไว้ และเพิ่ม indexes เข้าไป
    
    // ==========================================
    // ✅ INDEXES - เพิ่มส่วนนี้เข้าไปใน rules
    // ==========================================
    
    // ✅ BingoGame Cards Index (Phase 1) - Critical
    "games": {
      "$gameId": {
        "bingo": {
          "cards": {
            ".indexOn": ["userId"]
          }
        }
      }
    },
    
    // ✅ LiveChat Index (Phase 2) - Critical
    "chat": {
      "$gameId": {
        ".indexOn": ["timestamp"]
      }
    },
    
    // ✅ LoyKrathongGame Krathongs Index (Phase 6) - High Priority
    "krathongs": {
      "$gameId": {
        "recent": {
          ".indexOn": ["timestamp"]
        }
      }
    },
    
    // ==========================================
    // 🔒 SECURITY RULES - Rules ที่มีอยู่แล้ว
    // ==========================================
    // ⚠️ หมายเหตุ: Rules ด้านล่างนี้เป็นตัวอย่างเท่านั้น
    // ⚠️ ต้องปรับแต่งตามความต้องการของโปรเจกต์ของคุณ
    
    // ตัวอย่าง: อนุญาตให้ทุกคนอ่านและเขียนข้อมูล (สำหรับ development)
    // ⚠️ ⚠️ ⚠️ อย่าใช้ใน production! ต้องมี authentication และ authorization
    ".read": true,
    ".write": true
    
    // หรือถ้ามี authentication:
    // ".read": "auth != null",
    // ".write": "auth != null"
    
    // ตัวอย่าง: อนุญาตให้อ่านเฉพาะข้อมูลที่ authenticated
    // ".read": "auth != null",
    // ".write": "auth != null",
    // 
    // "users": {
    //   "$uid": {
    //     ".read": "$uid === auth.uid",
    //     ".write": "$uid === auth.uid"
    //   }
    // }
  }
}

