# 🧪 คู่มือการทดสอบ Phase 1 (Critical Issues)

## 📋 ภาพรวม

คู่มือนี้ใช้ทดสอบการแก้ไข Phase 1 ที่แก้ไขแล้ว 3 จุด:
1. **CheckinGame** - `answers/${gameId}` listener (90 วันล่าสุด)
2. **BingoGame** - `cards` listener (Query เฉพาะการ์ดของตัวเอง)
3. **CreateGame** - `USERS_EXTRA` listener (เปลี่ยนเป็น `get()`)

---

## 🛠️ การเตรียมการ

### 1. ตรวจสอบการแก้ไข Code
```bash
# ตรวจสอบว่าไฟล์ที่แก้ไขถูก commit แล้ว
git status

# ดู diff การเปลี่ยนแปลง
git diff HEAD~1 src/components/CheckinGame.tsx
git diff HEAD~1 src/components/BingoGame.tsx
git diff HEAD~1 src/pages/CreateGame.tsx
```

### 2. สร้าง Firebase Index (สำหรับ BingoGame)
⚠️ **สำคัญ:** ต้องสร้าง index ก่อนทดสอบ BingoGame

1. ไปที่ [Firebase Console](https://console.firebase.google.com/)
2. เลือกโปรเจกต์ของคุณ
3. ไปที่ **Realtime Database** → **Indexes**
4. คลิก **Create Index**
5. ตั้งค่า:
   - **Path:** `games/$gameId/bingo/cards`
   - **Key:** `userId`
   - **Order:** `ASC`
6. รอให้ index สร้างเสร็จ (อาจใช้เวลา 1-2 นาที)

### 3. เปิด Developer Tools
- Chrome: `F12` หรือ `Ctrl+Shift+I`
- Firefox: `F12` หรือ `Ctrl+Shift+K`
- Safari: `Cmd+Option+I`

เปิดแท็บ:
- **Console** - ดู logs และ errors
- **Network** - ตรวจสอบ network requests
- **Application** → **Local Storage** - ตรวจสอบ cache

---

## ✅ 1. ทดสอบ CheckinGame - Answers Listener

### 📍 สถานที่ทดสอบ:
- หน้าเล่นเกมเช็คอิน: `/play/{gameId}` (เกมประเภท "เกมเช็คอิน")

### 🧪 การทดสอบ:

#### ทดสอบ 1: ตรวจสอบโค้ดแสดงถูกต้อง
1. **เปิดหน้าเล่นเกมเช็คอิน**
   ```
   URL: http://localhost:3000/play/{gameId}
   ```
   - ใช้ gameId ของเกมเช็คอิน

2. **Login ด้วย username ที่เคยเช็คอินมาก่อน**
   - กรอก username
   - กรอก password
   - คลิก "ตกลง"

3. **ตรวจสอบว่าโค้ดแสดงถูกต้อง**
   - ดูที่ section "โค้ดที่ได้รับ" หรือ "รางวัล"
   - ควรแสดงโค้ดของวันที่เช็คอินก่อนหน้า
   - ตรวจสอบว่าโค้ดถูกต้อง (ตรงกับข้อมูลใน Firebase)

**ผลลัพธ์ที่คาดหวัง:**
- ✅ แสดงโค้ดได้ถูกต้อง
- ✅ ไม่มี error ใน Console
- ✅ โค้ดแสดงตรงกับข้อมูลใน Firebase

#### ทดสอบ 2: ตรวจสอบ Download (90 วัน)
1. **เปิด Developer Tools** → **Network**
2. **Filter:** `firebaseio.com`
3. **เช็คอินวันใหม่**
   - เช็คอินในวันใหม่
   - รอให้โค้ดแสดง

4. **ตรวจสอบ Network Requests**
   - ดู request ที่ไป `answers/{gameId}/{dateKey}`
   - ควรมี requests เฉพาะ dateKey ของ **90 วันล่าสุด** เท่านั้น
   - ไม่ควรมี requests สำหรับวันที่เก่ากว่า 90 วัน

**ผลลัพธ์ที่คาดหวัง:**
- ✅ มี requests เฉพาะ 90 วันล่าสุด
- ✅ ไม่มี requests สำหรับวันที่เก่ากว่า 90 วัน
- ✅ จำนวน requests น้อยลงมาก (ถ้ามีข้อมูลหลายเดือน)

#### ทดสอบ 3: ตรวจสอบ Console Logs
1. **เปิด Console**
2. **เช็คอินวันใหม่**
3. **ตรวจสอบ logs**
   - ควรไม่มี error เกี่ยวกับ `answers` listener
   - ควรไม่มี warning เกี่ยวกับ dateKey

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ไม่มี error
- ✅ ไม่มี warning (ยกเว้น warning ปกติ)

### 📊 วิธีตรวจสอบ Manual (ถ้าต้องการ):

```javascript
// เปิด Console และรันคำสั่งนี้เพื่อตรวจสอบ listeners
const checkListeners = () => {
  // ตรวจสอบว่า listeners ถูกสร้างสำหรับ 90 วันล่าสุด
  const today = new Date()
  const dateKeys = []
  for (let i = 0; i < 90; i++) {
    const date = new Date(today)
    date.setDate(date.getDate() - i)
    const year = date.getFullYear()
    const month = String(date.getMonth() + 1).padStart(2, '0')
    const day = String(date.getDate()).padStart(2, '0')
    dateKeys.push(`${year}${month}${day}`)
  }
  console.log('Expected dateKeys:', dateKeys.length, 'days')
  console.log('DateKeys:', dateKeys.slice(0, 5), '...', dateKeys.slice(-5))
}
checkListeners()
```

---

## ✅ 2. ทดสอบ BingoGame - Cards Listener

### 📍 สถานที่ทดสอบ:
- หน้าเล่นเกม BINGO: `/play/{gameId}` (เกมประเภท "เกม BINGO")

### 🧪 การทดสอบ:

#### ทดสอบ 1: ตรวจสอบ Index (ก่อนทดสอบ)
⚠️ **สำคัญ:** ต้องสร้าง index ก่อน

1. **ตรวจสอบว่า index สร้างแล้ว**
   - ไปที่ Firebase Console
   - Realtime Database → Indexes
   - ตรวจสอบว่า index `games/$gameId/bingo/cards` (key: `userId`) มีอยู่แล้ว

2. **ถ้ายังไม่มี index:**
   - สร้าง index ตามขั้นตอนใน "การเตรียมการ" ด้านบน
   - รอให้ index สร้างเสร็จ

#### ทดสอบ 2: ตรวจสอบการ์ดแสดงถูกต้อง
1. **เปิดหน้าเล่นเกม BINGO**
   ```
   URL: http://localhost:3000/play/{gameId}
   ```
   - ใช้ gameId ของเกม BINGO

2. **Login ด้วย username**
   - กรอก username
   - คลิก "ตกลง"

3. **ตรวจสอบว่าการ์ดแสดงถูกต้อง**
   - ควรแสดงการ์ดของตัวเองเท่านั้น
   - ตรวจสอบว่า checkedNumbers แสดงถูกต้อง
   - ลองติ๊กตัวเลขในการ์ด

**ผลลัพธ์ที่คาดหวัง:**
- ✅ แสดงการ์ดได้ถูกต้อง
- ✅ ไม่มี error ใน Console
- ✅ checkedNumbers อัพเดทถูกต้องเมื่อติ๊กตัวเลข

#### ทดสอบ 3: ตรวจสอบ Query ทำงานได้
1. **เปิด Developer Tools** → **Console**
2. **Login เข้าเกม BINGO**
3. **ตรวจสอบ Console Logs**
   - ควรไม่มี warning เกี่ยวกับ query หรือ index
   - ถ้ามี warning `⚠️ Bingo cards query requires index` แสดงว่า index ยังไม่สร้าง

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ไม่มี warning เกี่ยวกับ query
- ✅ Query ทำงานได้ (ไม่ต้องใช้ fallback)

#### ทดสอบ 4: ตรวจสอบ Download (เฉพาะการ์ดของตัวเอง)
1. **เปิด Developer Tools** → **Network**
2. **Filter:** `firebaseio.com`
3. **ติ๊กตัวเลขในการ์ด**
   - ติ๊กตัวเลขในตาราง 5x5
   - รอให้บันทึก

4. **ตรวจสอบ Network Requests**
   - ดู request ที่ไป `games/{gameId}/bingo/cards`
   - ควรได้เฉพาะการ์ดของตัวเองเท่านั้น
   - ไม่ควรได้การ์ดของคนอื่น

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ได้เฉพาะการ์ดของตัวเอง
- ✅ จำนวน download น้อยลงมาก (ถ้ามีผู้เล่นหลายคน)

#### ทดสอบ 5: ทดสอบ Fallback (ถ้าไม่มี index)
1. **ถ้า index ยังไม่สร้าง:**
   - ควรเห็น warning ใน Console: `⚠️ Bingo cards query requires index`
   - ควรใช้ fallback (listen ทั้งหมด) และ filter ใน client

2. **ตรวจสอบว่ายังทำงานได้:**
   - การ์ดยังแสดงถูกต้อง
   - checkedNumbers ยังอัพเดทได้
   - แต่ download มากกว่า (listen ทั้งหมด)

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ยังทำงานได้ (fallback)
- ⚠️ มี warning (ควรสร้าง index)

### 📊 วิธีตรวจสอบ Manual (ถ้าต้องการ):

```javascript
// เปิด Console และรันคำสั่งนี้เพื่อตรวจสอบ listeners
const checkBingoCards = () => {
  // ตรวจสอบว่า listeners ถูกสร้างสำหรับ cards ของตัวเองเท่านั้น
  console.log('ตรวจสอบ Bingo cards listener...')
  // ควรไม่มี error ถ้า query ทำงานได้
}
checkBingoCards()
```

---

## ✅ 3. ทดสอบ CreateGame - USERS_EXTRA Listener

### 📍 สถานที่ทดสอบ:
- หน้าสร้าง/แก้ไขเกม: `/creategame` หรือ `/games/{gameId}`

### 🧪 การทดสอบ:

#### ทดสอบ 1: ตรวจสอบรายชื่อ User แสดงถูกต้อง
1. **เปิดหน้าสร้าง/แก้ไขเกม**
   ```
   URL: http://localhost:3000/creategame
   หรือ
   URL: http://localhost:3000/games/{gameId}
   ```

2. **Login เข้าระบบ**
   - Login ด้วย admin account

3. **ตรวจสอบว่ารายชื่อ User แสดงถูกต้อง**
   - ดูที่ section "ผู้ใช้ทั้งหมด" หรือ dropdown เลือก user
   - ควรแสดงรายชื่อ user ทั้งหมดเรียงตาม hcoin (มากไปน้อย)
   - ตรวจสอบว่า hcoin แสดงถูกต้อง

**ผลลัพธ์ที่คาดหวัง:**
- ✅ แสดงรายชื่อ user ได้ถูกต้อง
- ✅ เรียงตาม hcoin ถูกต้อง
- ✅ hcoin แสดงถูกต้อง

#### ทดสอบ 2: ตรวจสอบใช้ `get()` ไม่ใช่ `onValue()`
1. **เปิด Developer Tools** → **Network**
2. **Filter:** `firebaseio.com`
3. **เปิดหน้าสร้าง/แก้ไขเกม**

4. **ตรวจสอบ Network Requests**
   - ควรมี **1 request** ไป `USERS_EXTRA` เมื่อโหลดหน้า
   - **ไม่ควรมี** requests ต่อเนื่อง (ไม่ใช่ listener)
   - ถ้ามี requests ต่อเนื่องแสดงว่ายังใช้ `onValue()` อยู่

**ผลลัพธ์ที่คาดหวัง:**
- ✅ มี 1 request เมื่อโหลดหน้า
- ✅ ไม่มี requests ต่อเนื่อง
- ✅ Download ลดลงมาก (ไม่ต้อง listen ทั้งหมด)

#### ทดสอบ 3: ตรวจสอบ Refresh เมื่อ Focus Window
1. **เปิดหน้าสร้าง/แก้ไขเกม**
   - รอให้โหลดเสร็จ

2. **เปิดหน้าอื่น (เช่น Facebook)**
   - Switch ไปหน้าอื่น
   - รอ 5-10 วินาที

3. **กลับมาหน้าสร้าง/แก้ไขเกม**
   - Switch กลับมา
   - รอ 1-2 วินาที

4. **ตรวจสอบ Network Requests**
   - ควรมี **request ใหม่** ไป `USERS_EXTRA` เมื่อ focus window
   - ตรวจสอบว่า hcoin อัพเดท (ถ้ามีการเปลี่ยนแปลง)

**ผลลัพธ์ที่คาดหวัง:**
- ✅ มี request ใหม่เมื่อ focus window
- ✅ hcoin อัพเดทถูกต้อง (ถ้ามีการเปลี่ยนแปลง)

#### ทดสอบ 4: ทดสอบข้อมูลอัพเดท
1. **เปิด 2 หน้าต่าง:**
   - หน้าต่าง 1: หน้าสร้าง/แก้ไขเกม
   - หน้าต่าง 2: หน้าเล่นเกมเช็คอิน

2. **ในหน้าต่าง 2:**
   - เช็คอินหรือทำอะไรที่ทำให้ hcoin เปลี่ยน

3. **กลับมาหน้าต่าง 1:**
   - Switch กลับมา
   - รอ 1-2 วินาที

4. **ตรวจสอบว่า hcoin อัพเดท**
   - hcoin ควรอัพเดทเป็นค่าใหม่
   - รายชื่อ user ควรเรียงใหม่ตาม hcoin

**ผลลัพธ์ที่คาดหวัง:**
- ✅ hcoin อัพเดทเมื่อ focus window
- ✅ รายชื่อ user เรียงใหม่ตาม hcoin

#### ทดสอบ 5: ตรวจสอบ Console Logs
1. **เปิด Console**
2. **เปิดหน้าสร้าง/แก้ไขเกม**
3. **ตรวจสอบ logs**
   - ควรไม่มี error เกี่ยวกับ `USERS_EXTRA` listener
   - ควรไม่มี warning

**ผลลัพธ์ที่คาดหวัง:**
- ✅ ไม่มี error
- ✅ ไม่มี warning

### 📊 วิธีตรวจสอบ Manual (ถ้าต้องการ):

```javascript
// เปิด Console และรันคำสั่งนี้เพื่อตรวจสอบ listeners
const checkUsersListener = () => {
  // ตรวจสอบว่าไม่มีการใช้ onValue() สำหรับ USERS_EXTRA
  console.log('ตรวจสอบ USERS_EXTRA listener...')
  // ควรไม่มี listener (ใช้ get() แทน)
}
checkUsersListener()
```

---

## 🔍 การตรวจสอบผลกระทบโดยรวม

### 1. ตรวจสอบ RTDB Download
1. **เปิด Firebase Console**
2. **ไปที่ Usage → Realtime Database**
3. **ตรวจสอบ Download**
   - ดูว่า download ลดลงหรือไม่
   - เปรียบเทียบก่อนและหลังการแก้ไข

### 2. ตรวจสอบ Performance
1. **เปิด Developer Tools** → **Performance**
2. **Record performance**
   - Record ระหว่างเล่นเกม
   - ตรวจสอบว่าไม่มี performance issues

### 3. ตรวจสอบ Console Errors
1. **เปิด Console**
2. **เล่นเกมทั้งหมด**
   - CheckinGame
   - BingoGame
   - CreateGame
3. **ตรวจสอบ errors**
   - ควรไม่มี error ใหม่
   - ควรไม่มี warning ใหม่

---

## 📝 Checklist การทดสอบ

### CheckinGame
- [ ] โค้ดแสดงถูกต้อง
- [ ] Download เฉพาะ 90 วันล่าสุด
- [ ] ไม่มี error ใน Console
- [ ] Network requests ถูกต้อง

### BingoGame
- [ ] สร้าง Firebase Index แล้ว
- [ ] การ์ดแสดงถูกต้อง
- [ ] Query ทำงานได้ (ไม่มี warning)
- [ ] Download เฉพาะการ์ดของตัวเอง
- [ ] checkedNumbers อัพเดทถูกต้อง

### CreateGame
- [ ] รายชื่อ user แสดงถูกต้อง
- [ ] ใช้ `get()` ไม่ใช่ `onValue()` (1 request)
- [ ] Refresh เมื่อ focus window
- [ ] hcoin อัพเดทถูกต้อง
- [ ] ไม่มี error ใน Console

---

## ⚠️ ปัญหาที่อาจพบ

### 1. BingoGame - Query ไม่ทำงาน
**อาการ:** เห็น warning `⚠️ Bingo cards query requires index`

**วิธีแก้ไข:**
1. สร้าง index ใน Firebase Console
2. รอให้ index สร้างเสร็จ (1-2 นาที)
3. Refresh หน้าเว็บ

### 2. CheckinGame - โค้ดไม่แสดง
**อาการ:** โค้ดไม่แสดงหรือแสดงไม่ถูกต้อง

**วิธีแก้ไข:**
1. ตรวจสอบว่า dateKey ถูกต้อง (90 วันล่าสุด)
2. ตรวจสอบ Console logs
3. ตรวจสอบว่าข้อมูลใน Firebase ถูกต้อง

### 3. CreateGame - รายชื่อ user ไม่อัพเดท
**อาการ:** รายชื่อ user ไม่อัพเดทเมื่อ focus window

**วิธีแก้ไข:**
1. ตรวจสอบว่า event listener ทำงาน (window focus)
2. ตรวจสอบ Console logs
3. ตรวจสอบว่ามี request ใหม่เมื่อ focus

---

## 📊 สรุปผลการทดสอบ

### สิ่งที่ควรเห็น:
- ✅ Download ลดลง **70-90%** สำหรับ 3 จุดนี้
- ✅ ไม่มี error ใหม่
- ✅ Performance ดีขึ้น
- ✅ ฟังก์ชันการทำงานยังเหมือนเดิม

### สิ่งที่ต้องทำเพิ่ม:
- ⚠️ **สร้าง Firebase Index** สำหรับ BingoGame
- ⚠️ **ทดสอบกับข้อมูลจริง** (หลาย user, หลายวัน)

---

## 🎯 ขั้นตอนต่อไป

ถ้าทดสอบผ่านแล้ว:
1. ✅ Commit และ push การแก้ไข
2. ✅ Deploy ขึ้น production (ถ้าพร้อม)
3. ✅ เริ่ม Phase 2 (High Priority Issues)

---

## 📞 ติดต่อ/รายงานปัญหา

ถ้าพบปัญหาหรือต้องการความช่วยเหลือ:
1. ตรวจสอบ Console logs
2. ตรวจสอบ Network requests
3. ตรวจสอบ Firebase Console
4. บันทึก error messages และ screenshots
