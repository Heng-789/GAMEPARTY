# Firestore Security Rules สำหรับ CheckinGame

## ⚠️ สำคัญ: ต้องอัพเดท Security Rules ใน Firebase Console

ไปที่ Firebase Console > Firestore Database > Rules และอัพเดทเป็น:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ✅ อนุญาตให้อ่าน/เขียนใน collection _temp (สำหรับดึง server timestamp)
    match /_temp/{document=**} {
      allow read, write: if true;
    }
    
    // ✅ อนุญาตให้อ่าน/เขียนใน collection _test (สำหรับทดสอบ)
    match /_test/{document=**} {
      allow read, write: if true;
    }
    
    // ✅ ตั้งค่า rules สำหรับ checkins (ตัวอย่าง - ปรับตามความต้องการ)
    match /checkins/{document=**} {
      allow read, write: if request.auth != null;
    }
    
    // ✅ ป้องกันการเข้าถึง collection อื่นๆ
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
```

## 📝 หมายเหตุ:

1. **Collection `_temp`**: ใช้สำหรับดึง server timestamp โดยอัตโนมัติ (จะถูกสร้างและลบทันที)
2. **Collection `_test`**: ใช้สำหรับทดสอบการเชื่อมต่อ
3. **Collection `checkins`**: ตัวอย่างสำหรับเก็บข้อมูลเช็คอิน (ปรับตามความต้องการ)

## 🔒 สำหรับ Production:

ควรตั้งค่า Security Rules ที่เข้มงวดกว่า เช่น:
- ตรวจสอบ authentication
- ตรวจสอบข้อมูลที่ส่งมา
- จำกัดการเข้าถึงตาม role/permission

